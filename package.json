{
  "name": "eisner-comic-reader",
  "version": "1.0.0",
  "description": "El lector de cómics que Will hubiera querido",
  "main": "index.js",
  "scripts": {
    "build": "echo 'Web assets ready'",
    "sync": "npx cap sync android",
    "open": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/core": "^5.7.0",
    "@capacitor/android": "^5.7.0",
    "@capacitor/filesystem": "^5.2.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.7.0"
  }
}
